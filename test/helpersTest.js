const helpers = require('../helpers');
const database = require('../database.js');

describe("emailExists", () => {

  it("returns true when looking for an email address that is already registered", () => {
    //helpers.emailExists(database.users,"johannes@drweber.de");
  });

});